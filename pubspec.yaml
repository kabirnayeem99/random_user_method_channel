name: random_user_method_channel
description: Flutter project to demonstrate random user profiling.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.3.3'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cached_network_image: ^3.2.2
  get: ^4.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true